import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const AddDevice = () => {
    return (
        <View>
            <Text>This is the Add Device Page</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default AddDevice;
